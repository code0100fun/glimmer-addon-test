'use strict';

module.exports = {
  name: 'glimmer-addon-test',

  // contentFor(type, config, content) {
  //   this._super(...arguments);

  //   console.log('contentFor!', type);
  // },

  // included(app) {
  //   this._super(...arguments);

  //   console.log('included!');
  // },

  // treeFor(type) {
  //   this._super(...arguments);

  //   console.log('treeFor!', type);
  // },

  // preprocessTree(type, tree) {
  //   this._super(...arguments);

  //   console.log('preprocessTree!', type);
  //   return tree;
  // },
};
